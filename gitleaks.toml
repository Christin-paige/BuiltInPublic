[allowlist]
description = "Allowlist for known safe secrets and files"

files = [
  "seeds.sql",
  "scripts/seeds/.*\\.ts",
  "\\.env\\.example"
]

paths = [
  "scripts/seeds",
  "supabase/fixtures"
]

commitAuthor = ["dependabot"]

[[allowlist.files]]
description = "Exclude autogenerated gitleaks report"
path = "gitleaks-report.json"
