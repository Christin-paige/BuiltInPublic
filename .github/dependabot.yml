version: 2
updates:
  # 🔐 SECURITY UPDATES — DAILY
  - package-ecosystem: 'npm'
    directory: '/' # root of the project
    schedule:
      interval: 'daily'
    allow:
      - dependency-type: 'direct'
    labels:
      - 'security'
    ignore:
      - dependency-name: 'eslint'
        versions: ['<8.0.0']

  # 🔧 REGULAR DEPENDENCIES — WEEKLY
  - package-ecosystem: 'npm'
    directory: '/' # root of the project
    schedule:
      interval: 'weekly'
    allow:
      - dependency-type: 'direct'
    labels:
      - 'dependencies'
    ignore:
      - dependency-name: 'eslint'
        versions: ['<8.0.0']
